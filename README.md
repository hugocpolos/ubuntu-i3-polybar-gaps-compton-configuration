# i3+i3gaps+polybar+comptom desktop configuration for debian based systems

@TO DO:
    Create an install.sh, to install all the required packages and move the config files to its correct places automatically.